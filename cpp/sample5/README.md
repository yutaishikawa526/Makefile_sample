## 各種コマンド
```bash
make setting
# または
sudo make setting
# を使用することで、clangとclang-formatのインストールが開始する

make all
# を使用することで、ソースコードのコンパイルとリンクが実行される

make format
# を使用することで、ソースコードのフォーマットが実行される

make clean
# を使用することで、コンパイルで作成されたオブジェクトファイルや実行ファイルが削除される
```